<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.rit.sfp.teachboard">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ActivityMain">

        </activity>
        <activity android:name=".WhiteboardActivity">

        </activity>
        <activity android:name=".CreateAccountActivity" android:noHistory="true"></activity>
        <activity android:name=".CreateBoardActivity" android:noHistory="true"></activity>
        <activity android:name=".TeachboardListActivity"></activity>
    </application>

</manifest>